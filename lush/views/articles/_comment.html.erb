<% default_gravatar = url_for :controller => 'images', :action => 'theme', :id => 'default_gravatar.png', :only_path => false %><% $alternater = params[:alternater].to_i if !params[:alternater].nil? %><% $alternater = $alternater.to_i + 1 %><li id="comment-<%= comment.id %>" class="comment<%= $alternater.remainder(2) %><%= ' author_comment' if comment.user %>" <%= 'style="display:none"' if controller.request.xhr? %><%= onhover_show_admin_tools(:comment, comment.id) %>><div class="commenthead"><em><%= $alternater %>.</em>&nbsp;(<%= distance_of_time_in_words @article.created_at, comment.created_at %> later)</div><div class="commenttext"><%= gravatar_tag(comment.email, :default => default_gravatar, :size => '30') if this_blog.use_gravatar and comment.email %><%= comment.html %><br style="clear:both;" /></div><p class="commentmeta"><cite><strong><%= (comment.url.blank?) ? h(comment.author) : link_to(h(comment.author), comment.url) %></strong></cite></p></li>
