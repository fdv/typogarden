<li id="comment-<%= comment.id %>" <%= 'style="display:none"' if controller.request.xhr? %><%= onhover_show_admin_tools(:comment, comment.id) %>>
  
  <a name="comment-<%= comment.id %>"></a>
  <div class="comment-head"<cite class="comment-author"><%= (comment.url.blank?) ? h(comment.author) : link_to(h(comment.author), comment.url) %></cite> <%= distance_of_time_in_words( @article.created_at, comment.created_at).capitalize %> later</div>
  <%= comment.html %>
</li>
